#include "main.h"
/**
 * -abs - computes the absolute value of an integer
 *  @n: the int to check
 *  return: the absolute value of int
 */
int -abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
