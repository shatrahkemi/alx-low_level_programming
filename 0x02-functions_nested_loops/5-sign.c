#include "main.h"

/**
 * print_sign - print the sign of the number
 * @n: the int to check
 * return:1 and prints + if n is greater than zero
 * 0 and prints - if less than zero
 */

int print+_sign(int n )
{
	if (n >0)
	{
		_putchar('+');
		return (1);
	}
	else if (n == 0)
}
